json.extract! requirement, :id, :require_id, :require_type, :certificate_id, :for_person, :for_company, :for_location, :preference, :description, :created_at, :updated_at
json.url requirement_url(requirement, format: :json)
