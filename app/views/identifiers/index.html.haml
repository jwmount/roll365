
.container-fluid

  %h1 Finder

  .row

    .col-2

      = render "search_panel", locals: {q: @q}
          
    .col-6
      %h3 Contacts
      = @identifiers.count 
      listed
      %hr

      %table.table.table-striped


        %thead
          %tr
            %th Orgaization or Person
            %th Name
            %th Value
            %th Rank or Priority
            

        %tbody
          - @identifiers.each do |iden|
            %tr
              %td{align: 'left', width: '224'}= link_to iden.identifiable.name, company_path(iden.identifiable_id)
              %td= iden.name
              %td{align: 'left', width: '224'}= iden.value
              %td{align: 'left', width: "64"}= iden.rank

            
              %td= link_to 'Show', iden
              %td= link_to 'Edit', edit_identifier_path(iden)
              %td= link_to 'Destroy', address, method: :delete, data: { confirm: "Are you sure you want to delete #{iden}?" } 
      %br
