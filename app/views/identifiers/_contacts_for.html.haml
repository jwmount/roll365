

- if @identifiers.nil?
  
  Identifiers
  = link_to 'New Contacts', new_person_identifiers(@person)

- else
  
  - @identifiers.each do |i|
    %p
    = i.rank.ordinalize + '--' + i.name + ': ' + i.value
  
  