

- if @company.nil?
  
  No affiliations found.
  
- else
  
- @company.people.each do |p|
  - @identifiers = p.identifiers
  .row
    .col-sm-2.data
      = p.display_name
    .col-sm-2.data
    
      - @identifiers.each do |i|
        = i.value
        = i.name

        = i.rank.ordinalize
    .row
